
# Diccionarios = Objeto(JSON)
# No estan ordenados 

dictionary = {
    'key': 'value',
    'a': [1, 2, 3],
    'b': 2,
    'verdad': True
}

print(dictionary['key']) # value, esto se ocupara para acceder al valor

user = {
    'name': 'Emilio',
    'country': 'Chile',
    'age': 28
}

print(user['name'])